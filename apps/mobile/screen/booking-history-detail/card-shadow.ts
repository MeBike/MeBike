import { colors } from "@theme/colors";

export const softCardShadowStyle = {
  shadowColor: colors.shadowColor,
  shadowOffset: { width: 0, height: 6 },
  shadowOpacity: 0.05,
  shadowRadius: 16,
  elevation: 2,
} as const;
