type: object
description: "Đối tượng đánh giá, bao gồm chi tiết các lý do được nhúng."
allOf:
  - $ref: './Rating.yaml' 
  - type: object
    properties:
      reason_details:
        type: array
        items:
          $ref: './RatingDetailReason.yaml'